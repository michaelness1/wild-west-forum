services:
  node:
    build:
      context: ./node
      dockerfile: Dockerfile
    container_name: wildwest-node
    ports:
      - "3000:3000"
    networks:
      - wildwest-net

  nginx:
    build:
      context: ./nginx
      dockerfile: Dockerfile
    container_name: wildwest-nginx
    depends_on:
      - node
    ports:
      - "8087:80"   # so you'll hit http://YOUR-IP:8087
    networks:
      - wildwest-net

networks:
  wildwest-net:
    driver: bridge